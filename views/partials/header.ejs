<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Express Basics</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/main.css">

        <script src="/js/jquery-3.3.1.slim.min.js"></script>
        <script src="/js/popper.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>

    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-sm navbar-light">
                <a class="navbar-brand" href="/clucks/"><img class="cluck-logo" src="../../images/logo.jpg"></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                        <% if ( username ) { %>
                            <a class="btn btn-secondary" href="/clucks/new">Cluck!</a>
                        <% }  else { %>
                        <a class="btn btn-secondary" href="/">Cluck!</a>
                        <% } %>
                        </li>
                    </ul>
                    <div class="navbar-box" >
                        <% if ( username ) { %>
                            <span class="navbar-text mr-2">Signed in as <%= username %></span>
                            <form action="/sign_out" method="POST" class="form-inline">
                                <button class="btn btn-secondary">Sign Out</button>
                            </form>
                        <% }  else { %>
                        <a class="btn btn-secondary" href="/" role="button">Sign In</a>
                        <% } %>
                    </div>
                </div>
            </nav>
        </header>
        <main class="container">